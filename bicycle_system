import numpy as np
import math

def bicycle_system(time, init_state):
    
    #Initial State
    X = init_state(0) #starting y coordinate
    Y = init_state(1) #starting y coordinate
    V = init_state(2) #initial velocity
    int_e_v = init_state(3)
    int_e_s = init_state(4)